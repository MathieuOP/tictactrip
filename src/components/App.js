// A FAIRE
// responsive (fait)
// ajouter la fleche (fait)
// opacity (fait)
// régler la view de droite si pas de recherche (fait)
// aucune ville trouvé (fait)
// factoriser les fichier (fait)

// valider et changer d'input avec la toucher entrer
// nettoyer le code




import React from 'react';
import './style/App.scss';
import Search from '../containers/Search';

const App = () => {

  return (
    <div className="app">
      <Search />
    </div>
  );
}

export default App;
